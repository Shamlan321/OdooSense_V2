# Frontend Environment Configuration
# Configure the backend API URL and port

# Option 1: Set complete API URL (overrides auto-detection)
# REACT_APP_API_URL=http://localhost:8000

# Option 2: Set only the port (uses current hostname with specified port)
REACT_APP_API_PORT=2222

# For network access from other machines, you can use:
# REACT_APP_API_URL=http://192.168.1.100:5000
# Replace 192.168.1.100 with your server's actual IP address